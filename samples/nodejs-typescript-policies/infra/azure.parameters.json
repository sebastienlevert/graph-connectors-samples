{
    "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
      "resourceBaseName": {
        "value": "rg-${{CONNECTOR_ID}}${{CONNECTOR_ID_SUFFIX}}{{RESOURCE_SUFFIX}}"
      },
      "appName": {
        "value": "${{APP_NAME}}"
      },
      "uniqueName": {
        "value": "${{CONNECTOR_ID}}${{CONNECTOR_ID_SUFFIX}}"
      }
    }
  }